<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jayu.sem7practicals">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sem7Practicals">
        <activity android:name=".activities.Pract9Activity"></activity>
        <activity android:name=".activities.Pract8Activity" />
        <activity android:name=".activities.Pract7Activity" />
        <activity android:name=".activities.Pract6Activity" />
        <activity android:name=".activities.Pract5Activity" />
        <activity android:name=".activities.Pract4Activity" />
        <activity android:name=".activities.Pract3SecondActivity" />
        <activity android:name=".activities.Pract3Activity" />
        <activity android:name=".activities.Pract1Activity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>